# Django project scaffold
# models.py, views.py, serializers.py, urls.py exist per convention

# !!! Because these are NOT real Django projects -- they are schema stubs
# for the agent to explore. The agent reads files, finds patterns,
# and makes decisions without actually running the server.

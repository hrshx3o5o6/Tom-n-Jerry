# Compatibility

Tom n Jerry is designed to travel across coding agents.


The repository does not require a runtime. Each `SKILL.md` file can be copied, adapted, or referenced by the host tool.

## Codex

Use each folder under `skills/` as a Codex skill.

Recommended flow:

1. Invoke `tom-core` when the task needs a candidate move.
2. Invoke `jerry-core` before implementation.
3. Invoke specialized Jerry skills when relevant.
4. Invoke `receipt-jerry` before claiming completion.

## Claude Code

Adapt skills as slash commands, project instructions, or memory fragments.

Suggested command mapping:

- `/tom` for `tom-core`
- `/jerry` for `jerry-core`
- `/receipt` for `receipt-jerry`
- `/tomnjerry` to trigger a global pre-flight opportunity scan (`jerry-core` + specialized scans)
- `/delete-jerry`, `/framework-jerry`, `/unix-jerry`, `/git-jerry`, `/browser-jerry`, `/dependency-jerry`, `/db-jerry`, `/api-jerry`, `/trap-jerry`, `/test-jerry` for narrower scans

## Harness Slash Command Configuration

To simplify setup, pre-made configuration templates are provided in the `templates/` directory.

### 1. Claude Code
Copy `templates/claudeproj.md` to your project root (or append its contents to your project instructions config file). This maps the `/tom`, `/jerry`, `/receipt`, and `/tomnjerry` commands.

### 2. Cursor (via `.cursorrules`)
Copy `templates/.cursorrules` to your project root. Cursor will automatically read this file and intercept edits, prompting the model to perform the pre-flight check and verify with receipts.

### 3. opencode
Copy `templates/opencode.json` to your project root. This instructs opencode to recursively register the standard and custom skill patterns.

### 4. Custom Harnesses
Intercept incoming prompt strings. If the input contains the token `/tomnjerry`, prep the prompt with the contents of `skills/jerry-core/SKILL.md` as system context, execute the scan, and return the Opportunity Cards to the user interface.



## opencode & Dynamic Loading

When configuring instruction paths or agent parameters in systems like `opencode`, ensure the environment is configured to read the entire local `skills/` directory recursively:
1. Load core steering skills (`tom-core`, `jerry-core`, `receipt-jerry`).
2. Load standard specialized Jerry skills.
3. **CRITICAL:** Dynamically scan and load any directory matching `skills/custom-*/SKILL.md` that has been auto-generated by the agent loop.

## Cursor, Aider, And Generic Agents

Copy the relevant skill text into rules, prompt files, `.cursorrules`, command snippets, or preflight checklists.

Use `docs/opportunity-cards.md` as the shared output contract.

## Minimal Manual Use

Even without tool support, a human can run the loop manually:

1. Ask the agent for its next candidate move.
2. Paste `jerry-core` and ask for opportunity cards.
3. If the codebase uses a framework/domain not covered by static skills, ask the agent to self-generate a `custom-<domain-name>` skill.
4. Choose the revised move.
5. Execute.
6. Paste `receipt-jerry` and require proof.

## Portability Rules

- Core skills should not assume one CLI, editor, or agent API.
- Runtime-specific guidance belongs in adapter docs, not the skills themselves.
- Skills should request evidence the host agent can actually inspect.
- Skills should degrade gracefully when a host lacks a tool.

